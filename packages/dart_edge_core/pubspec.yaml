name: dart_edge_core
description: Transport-agnostic core contracts for Dart Edge.
version: 0.3.45
publish_to: "https://registry.hippolabs.org/pub/internal-pub/"
repository: https://github.com/hippolabs-org/dart-http/tree/main/packages/dart_edge_core

environment:
  sdk: ">=3.13.0 <4.0.0"

resolution: workspace

dependencies:
  json_schema:
    hosted: https://pub.hippolabs.org
    version: ^0.1.2
  native_exchange:
    hosted: "https://registry.hippolabs.org/pub/internal-pub/"
    version: ^0.2.0

dev_dependencies:
  test: ^1.30.0
