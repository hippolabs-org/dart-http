name: Publish Packages

# GitHub Actions minutes are limited. Run this workflow manually and use it carefully.
on:
  workflow_dispatch:
    inputs:
      package_scope:
        description: Optional exact Melos package name to publish.
        required: false
        default: ""
      dry_run:
        description: Validate without publishing or creating native releases.
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  publish:
    uses: hipposphere/devops/.github/workflows/publish-dart-packages.yml@48675eed293989fce446b1ad72f5a3a336a0ceee
    with:
      package_scope: ${{ inputs.package_scope }}
      dry_run: ${{ inputs.dry_run }}
      dart_version: "3.13.0"
      devops_ref: 48675eed293989fce446b1ad72f5a3a336a0ceee
    secrets:
      pub_credentials: ${{ secrets.PUB_CREDENTIALS }}
      native_artifacts_token: ${{ secrets.NATIVE_ARTIFACTS_TOKEN }}
